interface:
  display_name: "Classroom Feedback"
  short_description: "Write structured parent-facing classroom feedback"
  default_prompt: "Use $classroom-feedback-skill to write feedback from this classroom screenshot."
policy:
  allow_implicit_invocation: true
